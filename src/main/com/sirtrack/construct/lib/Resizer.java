package com.sirtrack.construct.lib;

public interface Resizer {

	/**
	 * a function that takes the size of the subcon and "adjusts"
      or "resizes" it according to the encoding/decoding process.
	 */
	int resize( int length );
}
